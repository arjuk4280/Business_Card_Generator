const ids=['name','job','phone','email'];generateBtn.onclick=()=>{cardName.textContent=name.value||'Your Name';cardJob.textContent=job.value||'Job Title';cardPhone.textContent=phone.value||'+91';cardEmail.textContent=email.value||'mail@example.com';if(photo.files[0]){let r=new FileReader();r.onload=e=>cardPhoto.src=e.target.result;r.readAsDataURL(photo.files[0]);}};downloadBtn.onclick=()=>html2canvas(businessCard,{scale:2}).then(c=>{let a=document.createElement('a');a.download='Business_Card.png';a.href=c.toDataURL();a.click();});